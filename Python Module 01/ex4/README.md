Exercise 4 — Garden Security System

File: ft_garden_security.py

Spec:
- Protect plant data using encapsulation (protected attributes convention).
- Provide `set_height()`, `set_age()`, `get_height()`, `get_age()` with validation (no negatives).
- Print error messages and reject invalid updates.

Implementation uses protected attributes (`_name`, `_height`, `_days`) and the required setters/getters with validation and appropriate prints.
