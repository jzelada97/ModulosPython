Exercise 6 — Garden Analytics

File: ft_garden_analytics.py

Spec:
- Add `is_older_than_a_year` static method and `anonymous` class method.
- Implement `Seed` inheriting from `Flower` with seed counts after bloom.
- Each `Plant` should have a nested `Stats` class counting grow/age/show calls.
- Trees track `produce_shade()` calls. Provide a `display_stats` function.

Implementation includes `Plant.Stats`, `Tree.TreeStats`, `Seed`, static/class methods, and example outputs in `__main__`.
