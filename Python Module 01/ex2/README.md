Exercise 2 — Plant Growth Simulator

File: ft_plant_growth.py

Spec:
- Reuse `Plant` class; implement `grow()` and `age()` methods.
- Simulate 7 days of growth and show daily status and weekly growth summary.

Implementation includes `Plant` with `grow()`/`age()`, daily loop, and final growth printout.
