Exercise 1 — Garden Data Organizer

File: ft_garden_data.py

Spec:
- Create a `Plant` class modeling `name`, `height`, `age`.
- Provide a `show()` method to display plant info.
- Instantiate at least 3 plants and display them when run as a script.

This implementation provides `Plant` with `show()` and example instantiation in `__main__`.
