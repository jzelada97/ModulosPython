Exercise 0 — Planting Your First Seed

File: ft_garden_intro.py

Spec (from en.subject):
- Program should run when executed directly (`if __name__ == "__main__"`).
- Store variables: `name`, `height`, `age` and print formatted output using `print()`.
- No strict output check, but follow example display.

This file includes an example `if __name__ == "__main__"` block that prints the plant info. It is ready to run as a script.
