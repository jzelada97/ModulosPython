Exercise 1 — Archive Creation

Script: `ft_archive_creation.py`

Spec:
- Read file contents, append '#' to each line, display transformed content.
- Ask for a filename to save transformed data; create/replace the file if provided.
- Use `input()` for the save prompt.
