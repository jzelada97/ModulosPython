Exercise 2 — Stream Management

Script: `ft_stream_management.py`

Spec:
- Same as Archive Creation but print exception messages to `stderr` and obtain
  user input via `sys.stdin.readline()` instead of `input()`.
- Use `sys.stderr` for error messages and `sys.stdout` for prompts.
