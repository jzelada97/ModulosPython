Exercise 0 — Ancient Text Recovery

Script: `ft_ancient_text.py`

Spec:
- Read a filename from the command line and display its contents like `cat`.
- Print headers/footers and handle file access errors gracefully.
- Use `open()` and close the file; show a closing message.
