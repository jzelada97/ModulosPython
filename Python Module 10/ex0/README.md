Exercise 0 — Lambda Sanctum

File: `lambda_spells.py`

Functions:
- `artifact_sorter(artifacts)` — sort artifacts by `power` descending using `lambda`.
- `power_filter(mages, min_power)` — filter mages using `filter` with a `lambda`.
- `spell_transformer(spells)` — transform spell names using `map` with a `lambda`.
- `mage_stats(mages)` — compute max/min/avg using `lambda` with builtins.

Run:
python ex0/lambda_spells.py
