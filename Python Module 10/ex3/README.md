Exercise 3 — Ancient Library (functools)

File: `functools_artifacts.py`

Functions:
- `spell_reducer(spells, operation)` — uses `functools.reduce` and `operator`.
- `partial_enchanter(base_enchantment)` — returns partial variants.
- `memoized_fibonacci(n)` — uses `lru_cache` to memoize Fibonacci.
- `spell_dispatcher()` — returns a dispatcher function based on input type.

Run:
python ex3/functools_artifacts.py
