Exercise 2 — Memory Depths

File: `scope_mysteries.py`

Functions:
- `mage_counter()` — returns a closure counter using `nonlocal`.
- `spell_accumulator(initial_power)` — returns an accumulator closure.
- `enchantment_factory(enchantment_type)` — returns a function that applies the enchantment.
- `memory_vault()` — returns a dict with `store` and `recall` closures.

Run:
python ex2/scope_mysteries.py
