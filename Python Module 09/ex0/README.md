Exercise 0 — Space Station Data

File: `space_station.py`

Spec:
- Implement `SpaceStation` Pydantic model with field constraints.
- Provide a `main()` which demonstrates creation of a valid instance and an invalid one,
  showing Pydantic validation errors.

Run:
python ex0/space_station.py
