Exercise 3 — Making Your Own Error Types

Defines custom exceptions:
- `GardenError`
- `PlantError` (inherits GardenError)
- `WaterError` (inherits GardenError)

Includes functions to raise each custom error and tests showing they are caught individually and via the base `GardenError`.
