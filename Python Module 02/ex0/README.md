Exercise 0 — Agricultural Data Validation

Functions:
- `input_temperature(temp_str: str) -> int` : converts string to int
- `test_temperature() -> None` : runs tests with valid and invalid inputs and prints results

Spec: Convert strings to int, handle exceptions in `test_temperature` and keep program running.
