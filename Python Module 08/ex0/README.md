Exercise 0 — Entering the Matrix

Script: `construct.py`

Spec:
- Detect if running inside a virtual environment.
- Show current Python executable and environment path.
- Provide instructions to create/activate a venv when not inside one.
- Show site-packages path when inside a virtualenv.

Run:
python ex0/construct.py
