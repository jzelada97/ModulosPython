Exercise 2 — Accessing the Mainframe

Files: `oracle.py`, `.env.example`, `.gitignore`

Spec:
- Load configuration from environment variables, optionally from a `.env` file using `python-dotenv`.
- Show configuration differences for development/production and check for missing secrets.

Usage:
- Copy `.env.example` to `.env`, edit values, then run `python ex2/oracle.py`.
