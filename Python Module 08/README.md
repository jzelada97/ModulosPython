Module 08 — The Matrix (Virtual environments, package management, .env)

Implemented exercises:
- ex0/construct.py — detect virtualenv and show environment info.
- ex1/loading.py — dependency checker, data simulation with numpy, plotting (if deps present).
- ex1/requirements.txt and ex1/pyproject.toml provided.
- ex2/oracle.py — dotenv-backed config loader, .env.example and .gitignore provided.

Run examples: see each `ex*/README.md` for details.
