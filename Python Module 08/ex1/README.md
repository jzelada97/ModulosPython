Exercise 1 — Loading Programs

Files: `loading.py`, `requirements.txt`, `pyproject.toml`

Spec:
- Detect availability of `numpy`, `pandas`, `matplotlib`, `requests` and report status.
- Provide pip and Poetry installation instructions when packages are missing.
- When available, simulate data with `numpy`, analyze with `pandas` and plot with `matplotlib`.
- Save visualization to `matrix_analysis.png`.

Run:
python ex1/loading.py
