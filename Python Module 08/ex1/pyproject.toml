[tool.poetry]
name = "matrix-loading"
version = "0.1.0"
description = "Matrix data loading demo"

[tool.poetry.dependencies]
python = "^3.10"
numpy = "^1.25"
pandas = "^2.1"
matplotlib = "^3.7"
requests = "^2.31"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
