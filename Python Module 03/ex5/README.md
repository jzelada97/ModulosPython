Exercise 5 — Stream Wizard

Script: `ft_data_stream.py`

Spec:
- Implement an endless generator `gen_event()` producing (player, action) tuples.
- Generate 1000 events and display them.
- Build list of 10 events and implement `consume_event()` generator to yield random events until empty.
