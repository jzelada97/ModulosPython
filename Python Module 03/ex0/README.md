Exercise 0 — Command Quest

Script: `ft_command_quest.py`

Spec:
- Use `sys.argv` to display program name and arguments.
- Print counts and list arguments per spec.

This script implements the required behavior in `main()` and runs when executed as a script.
