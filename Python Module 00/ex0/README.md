Exercise 0 — Hello Garden

Function: `ft_hello_garden()`

Spec (from en.subject.txt):
- No arguments. Print the exact message: "Hello, Garden Community!".
- File must contain only the function definition and handle output directly.

This implementation: the file `ft_hello_garden.py` defines `ft_hello_garden()` and uses `print()` to output the message. It follows the exercise rules.

How I tested it:
- Ran a short script that imports the module, calls `ft_hello_garden()`, and captured stdout to verify the output matches the spec.
