Exercise 1 — Garden Name

Function: `ft_garden_name()`

Spec (from en.subject.txt):
- Must ask for garden name using `input()` with prompt exactly: "Enter garden name: ".
- Print two lines:
  Garden: <name>
  Status: Growing well!
- Handle input/output directly; file should contain only the function.

This implementation uses `input()` and `print()` exactly as specified.
