Exercise 2 — Garden Plot Area

Function: `ft_plot_area()`

Spec:
- Ask for `length` and `width` using `input()` prompts: "Enter length: ", "Enter width: ".
- Convert to `int` and print: "Plot area: <area>".
- Handle output directly in the function.

This implementation follows the spec using `int()` and `print()`.
