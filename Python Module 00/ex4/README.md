Exercise 4 — Plant Age Check

Function: `ft_plant_age()`

Spec:
- Prompt: "Enter plant age in days: " (int).
- If age > 60 print: "Plant is ready to harvest!"
- Else print: "Plant needs more time to grow."

Implementation follows the conditional exactly.
