Module 07 — DataDeck (Abstract Card Architecture)

Implemented packages:
- `ex0`: Creature base classes and factories (`FlameFactory`, `AquaFactory`).
- `ex1`: Capabilities (healing and transform) and corresponding factories (`HealingCreatureFactory`, `TransformCreatureFactory`).
- `ex2`: Strategy pattern implementation (`NormalStrategy`, `AggressiveStrategy`, `DefensiveStrategy`).

Test scripts:
- `battle.py`, `capacitor.py`, `tournament.py` demonstrate the required behaviors.
